import { makeStyles } from "@material-ui/core";

export const moviesContainer = makeStyles((theme)=> ({
    container:{
        display:"flex",
        justifyContent:"space-around",
        alignItems:"center",
        flexWrap:"wrap",
    },
    
}))
    